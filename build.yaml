build_in: build
type: img
name: kasm.img
clear_build_path: false
next:
- build: docker
	build_in: docker
	config:
		base_image: kasmweb/api:1.15.0
		image_name: eons/kasm.img
		image_os: debian
		install:
		- postgresql-client
		tags: 
		- "1.15.0"
		- latest
- build: docker_publish
	run_when_any:
	- release
	build_in: publish
	config:
		image_name: eons/kasm.img
